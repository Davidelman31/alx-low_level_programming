#include "main.h"
/**
 * _islower - function checks for lowercase character.
 * @c: the char to check.
 * Return: 1 if c is lowercase, otherwise 0
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
